## 做了什么

- 补充说明 OAG 节点分类（venue 标签）场景下 Macro/Micro-F1 偏低的常见根因：类别数极多 + 长尾极端、评估协议可能降级、以及 `min-class-support` 仅在 `label-cleanup-mode=eval_only` 时才生效。
- 给出“只提升 F1、尽量不影响 link/recon”等其他指标的改进手段（评估阶段过滤低支持类别、分类头 class_weight 与超参建议），并提供可直接复现实验的命令行示例。

## 变更前快照（问题表现）

- `all_results.csv` 中 EDANE/DANE 的 `macro_f1/micro_f1` 普遍很低，且用户容易误以为模型失效。
- 文档对“为什么低”“哪些参数真正生效”“如何在不影响 link/recon 的前提下改进”解释不足。

## 变更内容

- `docs/evaluation_setting_comparison.md`
  - 新增：F1 低的原因排查、`labels.csv` 长尾自检脚本、以及仅影响 F1 的推荐参数组合。
- `docs/pipeline_usage.md`
  - 新增：常见问题章节，强调 `label-cleanup-mode eval_only` 与 `min-class-support` 的关系，并给出两组推荐命令。

## 影响范围与风险

- 仅修改文档，不改代码逻辑；不影响现有实验结果与指标计算。
- 风险：读者可能将“提升 F1 的评估清洗”误解为“改变任务定义”。已在文档中提示其仅影响分类评估阶段统计。

## 验证

- 已本地阅读两份 Markdown，确认示例命令与参数名与 `src/edane_full_pipeline.py` 一致。
- 未进行额外代码运行（本次变更为文档更新）。

## 后续可选优化

- 若后续 `all_results.csv` 扩展为完整 `summary.json` 字段，可在汇总表中额外记录 `eval_class_count / labeled_nodes / f1_eval_protocol_used`，便于快速诊断 F1 低是长尾还是协议降级导致。
