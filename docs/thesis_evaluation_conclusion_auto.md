# 论文可用版评估结论（自动生成）

## 1. 总体结论

基于 `all_results.csv` 的 5 组数据集实验结果，EDANE 在结构保持、动态更新与量化压缩方面总体达到预期。
链路预测 AUC 覆盖区间为 **0.7427~0.9326**，均高于随机水平；量化压缩比稳定在 **7.99~7.99x**，说明压缩效果稳定。
初始化时延为 **49.38~101.13 ms**，平均增量更新时延为 **143.35~495.05 ms**，P95 更新时延为 **148.32~500.96 ms**。
分类指标方面，Macro-F1 区间 **0.0512~1.0000**，Micro-F1 区间 **0.2537~1.0000**，表明不同数据集在类别分布与特征可分性上存在显著差异。

其中，`amazon2m_sample` 在结构建模上表现最好（AUC=0.9326），`amazon2m_sample` 在总体分类准确性上表现最好（Micro-F1=1.0000），`amazon3m_sample` 的小类别识别能力最弱（Macro-F1=0.0512）。

## 2. 各数据集一句话解读

- `amazon2m_sample`：在 10000 节点规模下，最终 Macro-F1=1.0000、Micro-F1=1.0000、AUC=0.9326，平均更新时延=351.67 ms。
- `amazon3m_sample`：在 10000 节点规模下，最终 Macro-F1=0.0512、Micro-F1=0.8797、AUC=0.7821，平均更新时延=466.21 ms。
- `reddit_sample`：在 10000 节点规模下，最终 Macro-F1=0.1244、Micro-F1=0.2537、AUC=0.9095，平均更新时延=294.12 ms。
- `mag_sample`：在 8000 节点规模下，最终 Macro-F1=0.0748、Micro-F1=0.3558、AUC=0.8997，平均更新时延=143.35 ms。
- `twitter_sample`：在 10000 节点规模下，最终 Macro-F1=0.5530、Micro-F1=0.6300、AUC=0.7427，平均更新时延=495.05 ms。

## 3. 风险说明

- `amazon2m_sample`：未见显著单项风险，整体指标较均衡。
- `amazon3m_sample`：小类别识别能力偏弱（Macro-F1 较低）；类别不均衡影响明显（Micro-F1 与 Macro-F1 差距较大）。
- `reddit_sample`：未见显著单项风险，整体指标较均衡。
- `mag_sample`：小类别识别能力偏弱（Macro-F1 较低）。
- `twitter_sample`：结构可分性一般（AUC 偏低）。

## 4. 可直接引用的结论段（正文版）

综合实验结果可见，EDANE 在多数据集上均能稳定完成动态图嵌入学习，并在链路预测任务上取得稳定优势，说明其结构保持能力与增量更新机制有效。同时，int8 量化始终维持接近 8 倍压缩比，验证了该方法在存储效率上的工程可行性。然而，不同数据集的 Macro-F1 波动较大，反映模型在长尾类别上的鲁棒性仍有提升空间。后续可通过类别重加权、重采样、更高质量属性特征与分层评估策略进一步增强小类别识别能力。

> 自动生成时间：generate_thesis_conclusion.py 运行时；样本数：5；平均 AUC=0.8533，平均 Micro-F1=0.6238。
