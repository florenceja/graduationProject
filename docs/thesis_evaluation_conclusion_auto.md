# 论文可用版评估结论（自动生成）

## 1. 总体结论

基于 `all_results.csv` 的 5 组数据集实验结果，EDANE 在结构保持、动态更新与量化压缩方面总体达到预期。
链路预测 ROC-AUC 覆盖区间为 **0.7168~0.9161**，均高于随机水平；Link prediction Average Precision (AP) 覆盖区间为 **0.7667~0.9349**，Edge reconstruction ROC-AUC 覆盖区间为 **0.7053~0.9175**；量化压缩比稳定在 **7.99~7.99x**，说明压缩效果稳定。
初始化时延为 **192.90~669.99 ms**，平均增量更新时延为 **135.67~714.92 ms**，P95 更新时延为 **137.13~720.58 ms**。
分类指标方面，Macro-F1 区间 **0.0066~1.0000**，Micro-F1 区间 **0.0198~1.0000**，表明不同数据集在类别分布与特征可分性上存在显著差异。

其中，`amazon2m_sample` 在结构建模上表现最好（AUC=0.9161），`amazon2m_sample` 在真实边精准捕捉上表现最好（AP=0.9349），`amazon2m_sample` 在总体分类准确性上表现最好（Micro-F1=1.0000），`amazon3m_sample` 的小类别识别能力最弱（Macro-F1=0.0066）。

## 2. 各数据集一句话解读

- `amazon2m_sample`：在 10000 节点规模下，最终 Macro-F1=1.0000、Micro-F1=1.0000、AUC=0.9161、AP=0.9349、重构AUC=0.9175，平均更新时延=539.25 ms。
- `twitter_sample`：在 10000 节点规模下，最终 Macro-F1=0.1862、Micro-F1=0.4343、AUC=0.8564、AP=0.8552、重构AUC=0.8698，平均更新时延=714.92 ms。
- `reddit_sample`：在 10000 节点规模下，最终 Macro-F1=0.1344、Micro-F1=0.2610、AUC=0.8057、AP=0.8231、重构AUC=0.7897，平均更新时延=446.95 ms。
- `mag_sample`：在 8000 节点规模下，最终 Macro-F1=0.0681、Micro-F1=0.3658、AUC=0.7168、AP=0.7667、重构AUC=0.7053，平均更新时延=135.67 ms。
- `amazon3m_sample`：在 10000 节点规模下，最终 Macro-F1=0.0066、Micro-F1=0.0198、AUC=0.8580、AP=0.8627、重构AUC=0.8528，平均更新时延=687.72 ms。

## 3. 风险说明

- `amazon2m_sample`：未见显著单项风险，整体指标较均衡。
- `twitter_sample`：未见显著单项风险，整体指标较均衡。
- `reddit_sample`：未见显著单项风险，整体指标较均衡。
- `mag_sample`：小类别识别能力偏弱（Macro-F1 较低）；结构可分性一般（AUC 偏低）；网络重构能力一般（reconstruction AUC 偏低）。
- `amazon3m_sample`：小类别识别能力偏弱（Macro-F1 较低）。

## 4. 可直接引用的结论段（正文版）

综合实验结果可见，EDANE 在多数据集上均能稳定完成动态图嵌入学习，并在链路预测、Average Precision 与网络重构任务上取得稳定表现，说明其结构保持能力与增量更新机制有效。同时，int8 量化始终维持接近 8 倍压缩比，验证了该方法在存储效率上的工程可行性。然而，不同数据集的 Macro-F1 波动较大，反映模型在长尾类别上的鲁棒性仍有提升空间。后续可通过类别重加权、重采样、更高质量属性特征与分层评估策略进一步增强小类别识别能力。

> 自动生成时间：generate_thesis_conclusion.py 运行时；样本数：5；平均 AUC=0.8306，平均 AP=0.8485，平均重构AUC=0.8270，平均 Micro-F1=0.4162。
